pub const Tag = enum {
  Assign,
  Or,
  And,
  Pipe,
  Xor,
  BitAnd,
  Equal,
  NotEqual,
  Less,
  Greater,
  LessEq,
  GreaterEq,
  Shl,
  Shr,
  Add,
  AddAssign,
  Sub,
  SubAssign,
  Mul,
  MulAssign,
  Div, 
  DivAssign,
  BitNot,
  Not,
  Inc,
  Dec,
  IncNow,
  DecNow,
  LParen,
  RParen,
  LBracket,
  RBracket,
  LBrace,
  RBrace,
  Dot,
  DotOpt,
  Symbol,
  IntLit,
  BoolLit,
  FloatLit,
  CharLit,
  StringLit,
  NullLit,
  If,
  While,
  For,
  Colon,
  SemiColon,
};

pub const LexVal = union(enum) {
  pub IntLit: i128,
  pub CharLit: u8,
  pub BoolLit: bool,
  pub DoesntMatter: void
  // StringLit: just use the lexeme ya dolt
  // NullLit: whadd'ya think?

};

pub const Token = struct {
  pub lexeme: []const u8,
  pub val: LexVal = .DoesntMatter,
  pub tag: Tag,
};